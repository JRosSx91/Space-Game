use piston_window::*;

struct Spaceship {
    x: f64,
    y: f64,
    width: f64,
    height: f64,
    speed: f64,
}

struct Projectile {
    x: f64,
    y: f64,
    width: f64,
    height: f64,
    speed: f64,
}
fn main() {
    println!("Hello, world!");
}
